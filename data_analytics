<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1.0,user-scalable=no,maximum-scale=1.0" />
    <meta name="description" content="Portfolio of Mark Anthony Nene, aspiring Data Analyst with experience in Excel dashboards, audits, and dataset preprocessing.">
    <link rel="stylesheet" href="styles.css">
    <script src="scripts.js"></script>
    <title>Mark Anthony Nene — Data Analyst Portfolio</title>
</head>

<body>
    <div class="wrap">
        <header id="home">
            <div class="header-top">
                <div class="brand">
                    <a href="#home" id="profileLogo" class="logo" aria-label="Go to top of page">
                        <img src="https://uploads.onecompiler.io/43x5j6u93/43x5hb7dc/2X2%20-%201%20-%20Edited.png" alt="Mark Anthony Nene Profile Picture" loading="lazy">
                    </a>
                    <div>
                        <h1>Mark Anthony Nene</h1>
                        <div class="subtitle">Aspiring Data Analyst — Bachelor of Science in Applied Mathematics</div>
                    </div>
                </div>
                <div class="view-toggle-buttons">
                    <button id="mobileViewBtn" type="button" class="view-btn" aria-label="Toggle mobile view" aria-pressed="false">
                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
                            <title>Mobile View</title>
                            <path d="M17 2H7a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V4a2 2 0 0 0-2-2zm-1 18H8v-1a1 1 0 0 1 1-1h6a1 1 0 0 1 1 1v1zm1-3H7V5h10v12z" />
                        </svg>
                        <span class="view-label">Mobile View</span>
                    </button>
                    <button id="desktopViewBtn" type="button" class="view-btn active" aria-label="Toggle desktop view" aria-pressed="true">
                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
                            <title>Desktop View</title>
                            <path d="M22 17H2V5h20v12zm-2-2V7H4v8h16zM1 18h22v2H1v-2z" />
                        </svg>
                        <span class="view-label">Desktop View</span>
                    </button>
                </div>
            </div>
            <div class="hero-content">
                <h2>Turning raw data into insights that guide smarter decisions</h2>
                <p class="subtitle">Portfolio showcasing analytics, audits, preprocessing, and visualization projects</p>
                <div class="cta-row">
                    <a href="#projects" class="btn" aria-label="View Projects">View Projects</a>
                    <a id="downloadResume" href="https://drive.google.com/uc?export=download&id=145bViVMgn_5A1USynGsoquODSCgM_s_H" download="Mark_Anthony_Nene_Resume.pdf" class="btn-outline" target="_blank" rel="noopener" aria-label="Download Resume in PDF format">Download Resume (PDF)</a>
                </div>
            </div>
        </header>
        <section id="about-me">
            <div class="card-style">
                <h3 class="no-margin-top">About</h3>
                <p class="small">
                    I am a Bachelor of Science in Applied Mathematics student and aspiring Data Analyst passionate about using data to solve real-world problems.
                </p>
                <div class="small">
                    <strong>Selected Achievements:</strong>
                    <ul class="skills-list" style="list-style-type: disc; margin-left: 1.5em; padding-left: 0;">
                        <li>Built an interactive sales dashboard in Excel, identifying key revenue drivers and proposing discount strategies.</li>
                        <li>Cleaned and structured a Pima Diabetes dataset, improving data integrity for statistical analysis.</li>
                        <li>Audited a ₱2M+ student government budget, ensuring transparency and accountability.</li>
                    </ul>
                </div>
                <div>
                    <strong>Skills:</strong>
                    <ul class="skills-list" style="list-style-type: disc; margin-left: 1.5em; padding-left: 0;">
                        <li><strong>Tools:</strong> Excel, JASP, Praat</li>
                        <li><strong>Techniques:</strong> Data Cleaning, Data Curation, Pivot Tables, Statistical Analysis, Imputation</li>
                        <li><strong>Outputs:</strong> Dashboards, Audit Reports, Statistical Models</li>
                    </ul>
                </div>
            </div>
        </section>
        <section id="projects">
            <h2>Selected Projects</h2>
            <div class="grid">
                <article class="card" data-project-id="project1" tabindex="0">
                    <div class="card-summary">
                        <h4>Sales Dashboard</h4>
                        <p>Analyzed sales data to build a dynamic Excel dashboard, identifying key revenue drivers and suggesting optimized discount strategies for an e-commerce platform.</p>
                        <a href="#" class="link-btn" target="_blank" rel="noopener">View Project</a>
                    </div>
                    <div class="card-inner">
                        <div class="card-visual">
                            <svg width="100%" height="100%" viewBox="0 0 120 60" xmlns="http://www.w3.org/2000/svg">
                                <title>Sales Performance Chart</title>
                                <rect x="0" y="0" width="120" height="60" fill="transparent" />
                                <g transform="translate(8,8)" fill="none" stroke="#0ff" stroke-width="3" stroke-linecap="round">
                                    <path d="M0 48 L16 36 L32 44 L48 28 L64 34 L80 18 L96 28 L112 12" />
                                </g>
                            </svg>
                        </div>
                        <h3 class="p-title">Sales Performance Analytics Dashboard (Excel)</h3>
                        <p class="p-desc project-description">Built an interactive Excel dashboard. Cleaned raw transactions, used pivot tables and calculated fields to reveal revenue and discount impacts. Delivered recommendations for discount policy and resource allocation.</p>
                        <div class="p-actions">
                        </div>
                    </div>
                </article>
                <article class="card" data-project-id="project2" tabindex="0">
                    <div class="card-summary">
                        <h4>Pima Dataset</h4>
                        <p>Cleaned and prepared the Pima Diabetes dataset for statistical analysis using JASP, applying median imputation and removing outliers to ensure data integrity.</p>
                        <a href="#" class="link-btn" target="_blank" rel="noopener">View Project</a>
                    </div>
                    <div class="card-inner">
                        <div class="card-visual">
                            <svg width="100%" height="100%" viewBox="0 0 200 80" xmlns="http://www.w3.org/2000/svg">
                                <title>Medical data processing visual</title>
                                <rect x="0" y="0" width="200" height="80" fill="transparent" />
                                <g fill="none" stroke="#84cc16" stroke-width="2">
                                    <rect x="6" y="8" width="44" height="28" rx="4" />
                                    <rect x="56" y="8" width="44" height="28" rx="4" />
                                    <rect x="106" y="8" width="44" height="28" rx="4" />
                                    <line x1="0" y1="56" x2="200" y2="56" />
                                    <circle cx="30" cy="64" r="5" />
                                    <circle cx="70" cy="48" r="4" />
                                    <circle cx="110" cy="52" r="4" />
                                </g>
                            </svg>
                        </div>
                        <h3 class="p-title">Pima Diabetes Dataset Preprocessing (JASP)</h3>
                        <p class="p-desc project-description">Cleaned and standardized a medical dataset. Applied median imputation and IQR outlier removal. Prepared data for robust statistical analysis using JASP.</p>
                        <div class="p-actions">
                        </div>
                    </div>
                </article>
                <article class="card" data-project-id="project3" tabindex="0">
                    <div class="card-summary">
                        <h4>Budget Audit</h4>
                        <p>Audited a student government budget of over ₱2M, verifying transactions and creating transparent reports to enhance fiscal governance and accountability.</p>
                        <a href="#" class="link-btn" target="_blank" rel="noopener">View Project</a>
                    </div>
                    <div class="card-inner">
                        <div class="card-visual">
                            <svg width="100%" height="100%" viewBox="0 0 200 80" xmlns="http://www.w3.org/2000/svg">
                                <title>Financial audit visual</title>
                                <rect x="0" y="0" width="200" height="80" fill="transparent" />
                                <g fill="none" stroke="#06b6d4" stroke-width="2">
                                    <rect x="8" y="8" width="56" height="28" rx="4" />
                                    <rect x="86" y="8" width="56" height="28" rx="4" />
                                    <rect x="44" y="44" width="56" height="24" rx="4" />
                                    <path d="M16 20 L30 36 L46 14" stroke-linecap="round" stroke-width="3" />
                                </g>
                            </svg>
                        </div>
                        <h3 class="p-title">Financial Audit & Budget Transparency (₱2M+)</h3>
                        <p class="p-desc project-description">Audited a ₱2M+ student government budget. Verified transactions, reconciled records, and compiled transparent reports. Strengthened fiscal governance and trust.</p>
                        <div class="p-actions">
                        </div>
                    </div>
                </article>
                <article class="card" data-project-id="project4" tabindex="0">
                    <div class="card-summary">
                        <h4>Data Structuring</h4>
                        <p>Structured multilingual genealogical records and segmented audio using Praat, significantly increasing dataset usability and reducing downstream processing time.</p>
                        <a href="#" class="link-btn" target="_blank" rel="noopener">View Project</a>
                    </div>
                    <div class="card-inner">
                        <div class="card-visual">
                            <svg width="100%" height="100%" viewBox="0 0 200 80" xmlns="http://www.w3.org/2000/svg">
                                <title>Data cleaning and structuring visual</title>
                                <rect x="0" y="0" width="200" height="80" fill="transparent" />
                                <polyline points="4,60 18,42 36,48 54,30 72,40 90,24 108,34 126,20 144,44 170,30" fill="none" stroke="#9fb8c8" stroke-width="2" stroke-linecap="round" />
                            </svg>
                        </div>
                        <h3 class="p-title">Speech Segmentation & Genealogical Data Cleaning</h3>
                        <p class="p-desc project-description">Structured multilingual genealogical records and segmented audio using Praat. Increased dataset usability by 70–80% and reduced processing time for downstream models.</p>
                        <div class="p-actions">
                        </div>
                    </div>
                </article>
                <article class="card" data-project-id="project5" tabindex="0">
                    <div class="card-summary">
                        <h4>Future SQL Project</h4>
                        <p>A planned project showcasing advanced SQL queries to extract valuable insights from large, complex relational databases using joins, aggregation, and window functions.</p>
                        <a href="#" class="link-btn" target="_blank" rel="noopener">View Project</a>
                    </div>
                    <div class="card-inner">
                        <div class="card-visual flex-center-visual">
                            <svg width="60" height="60" viewBox="0 0 24 24">
                                <title>Future SQL project visual</title>
                                <path fill="#84cc16" d="M12 2L2 7l10 5 10-5z" />
                                <path fill="#06b6d4" d="M2 17l10 5 10-5" />
                            </svg>
                        </div>
                        <h3 class="p-title">Future SQL Project</h3>
                        <p class="p-desc project-description">Will showcase advanced SQL queries to extract insights from relational data (joins, aggregation, window functions).</p>
                        <div class="p-actions">
                        </div>
                    </div>
                </article>
                <article class="card" data-project-id="project6" tabindex="0">
                    <div class="card-summary">
                        <h4>Future Visualization</h4>
                        <p>A future project to present business metrics and user-driven filters in an interactive dashboard built with tools like Tableau or Power BI.</p>
                        <a href="#" class="link-btn" target="_blank" rel="noopener">View Project</a>
                    </div>
                    <div class="card-inner">
                        <div class="card-visual flex-center-visual">
                            <svg width="60" height="60" viewBox="0 0 24 24">
                                <title>Future Visualization project visual</title>
                                <circle cx="12" cy="12" r="10" fill="#071827" />
                                <rect x="6" y="8" width="3" height="10" fill="#84cc16" />
                                <rect x="11" y="5" width="3" height="13" fill="#06b6d4" />
                                <rect x="16" y="2" width="3" height="16" fill="#9fb8c8" />
                            </svg>
                        </div>
                        <h3 class="p-title">Future Visualization Project</h3>
                        <p class="p-desc project-description">Will present an interactive dashboard (Tableau/Power BI) showing business metrics and user-driven filters.</p>
                        <div class="p-actions">
                        </div>
                    </div>
                </article>
            </div>
        </section>
        <!-- New Contacts Section -->
        <section id="contacts">
            <h2>Contact Me</h2>
            <p class="small">Feel free to reach out to me for collaboration, inquiries, or just to say hi!</p>
            <div class="contacts-grid">
                <a href="mailto:markanthony.nene18@gmail.com" class="contact-item" aria-label="Email Mark Anthony Nene at markanthony.nene18@gmail.com">
                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
                        <title>Email icon</title>
                        <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2zm0 2v.5l8 6 8-6V6H4z" />
                    </svg>
                    <h3 class="p-title">Email</h3>
                    <p class="p-desc">markanthony.nene18@gmail.com</p>
                </a>
                <a href="https://www.linkedin.com/in/mark-anthony-nene-072964371/" target="_blank" class="contact-item" aria-label="View Mark Anthony Nene's profile on LinkedIn">
                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
                        <title>LinkedIn icon</title>
                        <path d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.761 0 5-2.239 5-5v-14c0-2.761-2.239-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.766s.784-1.767 1.75-1.767 1.75.791 1.75 1.767-.783 1.766-1.75 1.766zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.671 7 2.358v6.877z" />
                    </svg>
                    <h3 class="p-title">LinkedIn</h3>
                    <p class="p-desc">linkedin.com/in/mark-anthony-nene</p>
                </a>
                <a href="https://github.com/yourgithub" target="_blank" class="contact-item" aria-label="View Mark Anthony Nene's profile on GitHub">
                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
                        <title>GitHub icon</title>
                        <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.809 1.309 3.492.997.107-.775.418-1.309.762-1.605-2.665-.299-5.466-1.332-5.466-5.93 0-1.314.465-2.384 1.235-3.221-.135-.303-.541-1.523.111-3.176 0 0 1.006-.322 3.301 1.231.956-.266 1.983-.399 3.003-.404 1.02.005 2.046.138 3.006.404 2.291-1.553 3.297-1.231 3.297-1.231.654 1.653.248 2.873.111 3.176.77.837 1.233 1.907 1.233 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.197-6.091 8.197-11.385 0-6.627-5.373-12-12-12z" />
                    </svg>
                    <h3 class="p-title">GitHub</h3>
                    <p class="p-desc">github.com/yourgithub</p>
                </a>
            </div>
        </section>
        <!-- Modals -->
        <div class="modal-back" aria-hidden="true" role="dialog" aria-labelledby="modalTitle">
            <div class="modal">
                <h3 id="modalTitle"></h3>
                <p class="small" id="modalText"></p>
                <div class="flex" style="display:flex; justify-content: flex-end; gap:8px;">
                    <button class="close" id="modalClose" aria-label="Close project details">Close</button>
                </div>
            </div>
        </div>
        <div class="profile-modal-back" aria-hidden="true" role="dialog" aria-label="Profile image dialog">
            <div class="profile-modal-content" tabindex="-1">
                <button class="profile-modal-close" aria-label="Close profile picture modal">×</button>
                <img src="https://uploads.onecompiler.io/43x5j6u93/43x5hb7dc/2X2%20-%201%20-%20Edited.png" alt="Mark Anthony Nene Profile Picture" loading="lazy">
            </div>
        </div>
    </div>
    <!-- Floating Navigation Bar -->
    <div class="floating-nav-wrapper">
        <nav class="floating-nav" aria-label="Main navigation">
            <a href="#home">Home</a>
            <a href="#about-me">About</a>
            <a href="#projects">Projects</a>
            <a href="#contacts">Contacts</a>
        </nav>
    </div>
    <button id="scrollTopBtn" aria-label="Scroll to top of page">↑</button>
    <footer>
        <p>&copy; 2024 Mark Anthony Nene. All Rights Reserved.</p>
    </footer>
</body>

</html>
// scripts.js - robust toggle + diagnostics + persistence
(function () {
  'use strict';

  // Global error catcher to help debugging
  window.addEventListener('error', function (e) {
    console.error('[scripts.js] window error', e.error || e.message, e.filename, 'line', e.lineno);
  });

  document.addEventListener('DOMContentLoaded', () => {
    const $ = sel => document.querySelector(sel);
    const $$ = sel => Array.from(document.querySelectorAll(sel));
    const log = (...args) => {
      if (window.console && console.info) console.info('[scripts.js]', ...args);
    };

    log('DOM ready');

    // ===== ELEMENTS =====
    const mobileViewBtn = $('#mobileViewBtn');
    const desktopViewBtn = $('#desktopViewBtn');
    const body = document.body;
    const scrollBtn = $('#scrollTopBtn');

    log('elements', { mobileViewBtn, desktopViewBtn, body, scrollBtn });

    // Defensive: if elements missing, try fallback selectors
    if (!mobileViewBtn) {
      const m = document.querySelector('.view-toggle-buttons button:first-child');
      if (m) {
        log('mobileViewBtn not found by id; using fallback', m);
      }
    }
    if (!desktopViewBtn) {
      const d = document.querySelector('.view-toggle-buttons button:last-child');
      if (d) {
        log('desktopViewBtn not found by id; using fallback', d);
      }
    }

    // Helper to update UI + aria + storage
    function setViewState({ mobile }) {
      try {
        if (mobile) {
          body.classList.add('mobile-view');
          if (mobileViewBtn) { mobileViewBtn.classList.add('active'); mobileViewBtn.setAttribute('aria-pressed', 'true'); }
          if (desktopViewBtn) { desktopViewBtn.classList.remove('active'); desktopViewBtn.setAttribute('aria-pressed', 'false'); }
          localStorage.setItem('preferredView', 'mobile');
          log('setViewState -> mobile');
        } else {
          body.classList.remove('mobile-view');
          if (desktopViewBtn) { desktopViewBtn.classList.add('active'); desktopViewBtn.setAttribute('aria-pressed', 'true'); }
          if (mobileViewBtn) { mobileViewBtn.classList.remove('active'); mobileViewBtn.setAttribute('aria-pressed', 'false'); }
          localStorage.setItem('preferredView', 'desktop');
          log('setViewState -> desktop');
        }
      } catch (err) {
        console.error('[scripts.js] setViewState error', err);
      }
    }

    // Initialize from localStorage or markup
    try {
      const pref = localStorage.getItem('preferredView');
      if (pref === 'mobile') setViewState({ mobile: true });
      else if (pref === 'desktop') setViewState({ mobile: false });
      else {
        // fall back to markup: if desktop button has .active or aria-pressed true
        const desktopPressed = desktopViewBtn && (desktopViewBtn.classList.contains('active') || desktopViewBtn.getAttribute('aria-pressed') === 'true');
        setViewState({ mobile: !desktopPressed });
      }
    } catch (err) {
      console.error('[scripts.js] init localStorage error', err);
    }

    // Attach listeners safely
    function attach(btn, fn) {
      if (!btn) return;
      // pointerup covers touch and mouse; click as fallback
      btn.addEventListener('pointerup', fn, { passive: true });
      btn.addEventListener('click', fn, { passive: true });
      btn.addEventListener('keydown', (ev) => {
        if (ev.key === 'Enter' || ev.key === ' ') {
          ev.preventDefault();
          fn(ev);
        }
      });
      // add a small aria toggle when activated by keyboard or click
      btn.setAttribute('role', 'button');
    }

    // Guard to prevent ultra-rapid toggles
    let lastToggle = 0;
    function guard(fn) {
      return function (ev) {
        const now = Date.now();
        if (now - lastToggle < 120) return;
        lastToggle = now;
        try { fn(ev); } catch (err) { console.error('[scripts.js] guarded handler error', err); }
      };
    }

    attach(mobileViewBtn, guard((ev) => {
      ev && ev.preventDefault && ev.preventDefault();
      setViewState({ mobile: true });
    }));
    attach(desktopViewBtn, guard((ev) => {
      ev && ev.preventDefault && ev.preventDefault();
      setViewState({ mobile: false });
    }));

    // Expose a helper for console testing
    window.debugToggleMobile = function () {
      const isMobile = document.body.classList.contains('mobile-view');
      setViewState({ mobile: !isMobile });
      log('debugToggleMobile -> now mobile?', document.body.classList.contains('mobile-view'));
    };

    log('listeners attached. Try window.debugToggleMobile() or click the buttons.');

    // Optional: quick visual sanity log when body.mobile-view class changes
    new MutationObserver((mutations) => {
      mutations.forEach(m => {
        if (m.attributeName === 'class') {
          log('body class changed:', body.className);
        }
      });
    }).observe(body, { attributes: true, attributeFilter: ['class'] });

    // End of DOMContentLoaded
  }); // DOMContentLoaded
})(); // IIFE
/* ========================================================================= */
/* BASE & GLOBAL STYLES */
/* ========================================================================= */
:root{
  --bg:#0f1724;
  --card:#0b1220;
  --muted:#9aa8b6;
  --accent:#06b6d4;
  --accent-2:#84cc16;
  --glass:rgba(255,255,255,0.03);
  --radius:14px;
  --maxw:1280px;
  font-family:system-ui,-apple-system,"Segoe UI",Roboto,"Helvetica Neue",Arial;
  scroll-behavior:smooth;
}
html,body{
  height:100%;
  margin:0;
  color:#ecf0f3;
  background:#071021;
  overflow-x:hidden;
  scroll-behavior:smooth;
}
body::before{
  content:'';
  position:fixed;
  top:0;
  left:0;
  width:100%;
  height:100%;
  background:radial-gradient(circle at 10% 20%, #0c1a2c, #0b1220 80%);
  animation:pulse 10s ease-in-out infinite;
  z-index:-1;
  opacity:0.8;
}
@keyframes pulse{
  0%{transform:scale(1.0);}
  50%{transform:scale(1.02);}
  100%{transform:scale(1.0);}
}
section,header{
  opacity:0;
  transform:translateY(20px);
  animation:fadeIn 0.6s ease-out forwards;
}
@keyframes fadeIn{ to{ opacity:1; transform:translateY(0); } }
header{ animation-delay:0s; }
#about-me{ animation-delay:0.2s; }
#projects{ animation-delay:0.4s; }
#contacts{ animation-delay:0.6s; }
footer{ animation-delay:0.8s; }

/* Disable animations for reduced-motion users */
@media (prefers-reduced-motion: reduce){
  *{ transition:none !important; animation:none !important; }
}

/* Focus state for accessibility */
:focus{ outline:2px solid var(--accent); outline-offset:3px; }
.view-btn:focus, .link-btn:focus, .btn:focus{ outline:2px solid var(--accent); outline-offset:2px; }

/* Layout wrapper */
.wrap{
  width:100%;
  padding:clamp(1.5rem,5vw,2.5rem);
  max-width:var(--maxw);
  margin:0 auto;
  transition:max-width 0.3s ease-in-out, padding 0.3s ease-in-out;
}

/* ========================================================================= */
/* HEADER & NAVIGATION */
/* ========================================================================= */
header{
  display:flex;
  flex-direction:column;
  gap:24px;
  background:var(--bg);
  padding:12px clamp(1.5rem,5vw,2.5rem);
  margin-bottom:20px;
}
.header-top{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:16px;
}
.brand{
  display:flex;
  align-items:center;
  gap:12px;
}
.logo{
  width:54px;
  height:54px;
  border-radius:10px;
  background:linear-gradient(135deg,var(--accent),var(--accent-2));
  display:flex;
  align-items:center;
  justify-content:center;
  font-weight:700;
  color:#04202a;
  overflow:hidden;
  padding:0;
  cursor:pointer;
}
.logo img{
  display:block;
  width:100%;
  height:100%;
  object-fit:cover;
  border-radius:10px;
  transition:transform 0.3s ease;
}
.logo:hover img{ transform:scale(1.05); }

h1{ font-size:24px; margin:0; }
.subtitle{ color:var(--muted); font-size:16px; margin-top:4px; }

/* View toggle container */
.view-toggle-buttons{
  display:flex;
  gap:10px;
  margin-left:auto;
}

/* Consolidated view button styles (single source of truth) */
.view-btn{
  -webkit-appearance:none;
  appearance:none;
  touch-action:manipulation;
  -webkit-tap-highlight-color:rgba(0,0,0,0.06);
  background:#06151a;
  color:var(--muted);
  border:1px solid rgba(255,255,255,0.06);
  padding:8px 12px;
  border-radius:8px;
  font-weight:600;
  cursor:pointer;
  transition:all 0.18s ease;
  display:flex;
  align-items:center;
  justify-content:center;
  position:relative;
  user-select:none;
}

/* aria-pressed is authoritative: when true show active state */
.view-btn[aria-pressed="true"]{
  background:var(--accent);
  color:#04202a;
  box-shadow:0 0 12px rgba(6,182,212,0.85);
  transform:translateY(-3px);
}

/* Hover fallback for pointer users */
.view-btn:hover{ transform:translateY(-3px); box-shadow:0 0 12px rgba(6,182,212,0.8); background:var(--accent); color:#04202a; }

/* Icon sizing */
.view-btn svg{ width:24px; height:24px; transition:fill 0.18s; }
.view-btn[aria-pressed="true"] svg,
.view-btn:hover svg{ fill:#04202a; }

/* Hover label */
.view-label{
  position:absolute;
  top:-30px;
  padding:4px 8px;
  background:#1e293b;
  color:#fff;
  font-size:12px;
  border-radius:4px;
  white-space:nowrap;
  opacity:0;
  transition:opacity 0.18s ease;
  pointer-events:none;
}
.view-btn:hover .view-label{ opacity:1; }

/* Keyboard focus-visible */
.view-btn:focus-visible{
  outline:3px solid rgba(6,182,212,0.22);
  outline-offset:3px;
  box-shadow:0 0 0 4px rgba(6,182,212,0.08);
}

/* Hero content */
.hero-content{ display:flex; flex-direction:column; gap:12px; }
.hero-content h2{ font-size:clamp(28px,5vw,42px); margin:0; }
.hero-content .subtitle{ font-size:clamp(16px,2.5vw,20px); margin-top:0; }

.cta-row{ margin-top:14px; display:flex; gap:10px; flex-wrap:wrap; }
.btn, .btn-outline{
  padding:12px 18px;
  border-radius:10px;
  text-decoration:none;
  font-weight:600;
  font-size:16px;
  display:inline-block;
  cursor:pointer;
  transition:all 0.3s ease;
}
.btn{ background:var(--accent); color:#04202a; }
.btn:hover{ box-shadow:0 0 12px rgba(6,182,212,0.8); transform:translateY(-3px); }
.btn-outline{
  background:transparent;
  border:1px solid rgba(255,255,255,0.06);
  color:var(--muted);
}
.btn-outline:hover{ box-shadow:0 0 12px rgba(255,255,255,0.4); transform:translateY(-3px); }

/* ========================================================================= */
/* PROJECTS GRID & CARDS */
/* ========================================================================= */
.grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(300px,1fr));
  gap:24px;
  margin-top:24px;
}
.card, .card-style{
  background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
  padding:18px;
  border-radius:var(--radius);
  box-shadow:0 6px 16px rgba(2,6,23,0.6);
  border:1px solid rgba(255,255,255,0.06);
}
.card{
  transition:transform 0.3s ease, box-shadow 0.3s ease, border-color 0.3s ease;
  cursor:pointer;
  display:flex;
  flex-direction:column;
  position:relative;
  overflow:hidden;
  height:380px;
}
.card-inner{
  position:absolute;
  inset:0;
  display:flex;
  flex-direction:column;
  padding:18px;
  transition:all 0.4s cubic-bezier(0.175,0.885,0.32,1.275);
}
.card:hover .card-inner{ transform:translateY(-6px) scale(1.02) rotateX(2deg); opacity:0; }
.card-summary{
  position:absolute;
  inset:0;
  padding:18px;
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  background:linear-gradient(135deg, var(--accent), var(--accent-2));
  color:#04202a;
  border-radius:var(--radius);
  opacity:0;
  transition:opacity 0.4s ease;
}
.card:hover .card-summary{ opacity:1; }
.card-summary h4{ margin-top:0; font-size:24px; font-weight:700; }
.card-summary p{ text-align:center; font-size:16px; line-height:1.4; }

.card:hover{ box-shadow:0 0 25px rgba(6,182,212,0.6), 0 0 40px rgba(6,182,212,0.3); border-color:var(--accent); }

.card-visual{
  height:180px;
  border-radius:10px;
  overflow:hidden;
  background:linear-gradient(135deg,#071226 0%, #071827 100%);
  display:flex;
  align-items:center;
  justify-content:center;
  margin-bottom:12px;
  color:var(--accent);
  font-size:28px;
  font-weight:bold;
}
.flex-center-visual{ display:flex; align-items:center; justify-content:center; }
.card-visual svg{ width:100%; height:100%; }

.p-title{ font-weight:700; margin:0 0 6px 0; }
.p-desc{ color:var(--muted); font-size:14px; margin:0 0 12px 0; min-height:48px; }
.project-description{ min-height:48px; }
.p-actions{ display:flex; gap:10px; align-items:center; margin-top:auto; }

.link-btn{
  background:#06151a;
  color:var(--accent);
  border:1px solid rgba(6,182,212,0.08);
  padding:8px 12px;
  border-radius:8px;
  text-decoration:none;
  font-weight:600;
  cursor:pointer;
  transition:all 0.3s ease;
}
.link-btn:hover{ box-shadow:0 0 12px rgba(6,182,212,0.8); transform:translateY(-3px); }

/* Contacts */
.contacts-grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(200px,1fr));
  gap:16px;
  margin-top:16px;
}
.contact-item{
  display:flex;
  flex-direction:column;
  align-items:center;
  text-align:center;
  padding:20px;
  background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
  border-radius:var(--radius);
  border:1px solid rgba(255,255,255,0.06);
  transition:transform 0.3s ease, box-shadow 0.3s ease;
  text-decoration:none;
  color:inherit;
}
.contact-item:hover{
  transform:translateY(-6px);
  box-shadow:0 0 25px rgba(6,182,212,0.8), 0 0 50px rgba(6,182,212,0.6);
  border-color:var(--accent);
}
.contact-item svg{ width:48px; height:48px; margin-bottom:12px; color:var(--accent); }

/* ========================================================================= */
/* FOOTER */
/* ========================================================================= */
footer{
  margin-top:28px;
  color:var(--muted);
  font-size:13px;
  text-align:center;
  padding:18px 0 100px 0;
}

/* ========================================================================= */
/* MODALS */
/* ========================================================================= */
.modal-back, .profile-modal-back{
  position:fixed;
  inset:0;
  background:rgba(2,6,23,0.7);
  display:flex;
  align-items:center;
  justify-content:center;
  z-index:1000;
  opacity:0;
  pointer-events:none;
  transition:opacity 0.3s ease;
}
.modal-back.show, .profile-modal-back.show{ opacity:1; pointer-events:auto; }

.modal{
  background:#081222;
  padding:18px;
  border-radius:12px;
  max-width:420px;
  width:92%;
  box-shadow:0 10px 40px rgba(2,6,23,0.8);
  color:#eaf6f8;
  position:relative;
}
.modal .close{
  background:transparent;
  border:none;
  color:var(--muted);
  font-size:13px;
  padding:8px 10px;
  border-radius:8px;
  cursor:pointer;
}
.profile-modal-content{
  position:relative;
  width:min(50vh,50vw);
  height:min(50vh,50vw);
  padding:clamp(1rem,5vw,2rem);
  background:rgba(0,0,0,0.4);
  border-radius:12px;
  backdrop-filter:blur(10px);
  overflow-y:auto;
}
.profile-modal-content img{ display:block; width:100%; height:100%; object-fit:contain; border-radius:10px; }
.profile-modal-close{
  position:absolute;
  top:0.5rem;
  right:0.5rem;
  background:rgba(255,255,255,0.1);
  color:#fff;
  font-size:1.5rem;
  font-weight:bold;
  border:none;
  border-radius:50%;
  width:2rem;
  height:2rem;
  display:flex;
  align-items:center;
  justify-content:center;
  cursor:pointer;
  transition:background-color 0.2s;
}
.profile-modal-close:hover{ background:rgba(255,255,255,0.2); }

/* ========================================================================= */
/* DYNAMIC FLOATING NAV */
/* ========================================================================= */
.floating-nav-wrapper{
  position:fixed;
  bottom:20px;
  left:0;
  right:0;
  z-index:50;
  transition:all 0.3s ease;
  opacity:1;
  transform:translateY(0);
}
.floating-nav-wrapper.minimized .floating-nav{ padding:8px 16px; }
.floating-nav-wrapper.minimized a{ font-size:14px; }
.floating-nav-wrapper.hidden-nav{ opacity:0; transform:translateY(100px); pointer-events:none; }
.floating-nav-wrapper:hover.minimized{ transform:translateY(0); opacity:1; }

.floating-nav{
  display:flex;
  justify-content:center;
  max-width:var(--maxw);
  margin:0 auto;
  padding:12px 32px;
  background:rgba(15,23,36,0.9);
  backdrop-filter:blur(10px);
  border-radius:999px;
  box-shadow:0 0 15px rgba(6,182,212,0.4);
  border:1px solid rgba(6,182,212,0.2);
  pointer-events:auto;
  width:fit-content;
  gap:16px;
  transition:all 0.3s ease;
}
.floating-nav a{
  padding:8px 16px;
  border-radius:999px;
  text-decoration:none;
  color:var(--muted);
  font-weight:600;
  font-size:15px;
  transition:all 0.3s ease;
}
.floating-nav a:hover, .floating-nav a.active{
  background:var(--accent);
  color:#04202a;
  box-shadow:0 0 12px rgba(6,182,212,0.8);
  transform:translateY(-2px);
}

/* ========================================================================= */
/* RESPONSIVE STYLES */
/* ========================================================================= */
@media (max-width:640px){
  header{ flex-direction:column; align-items:flex-start; gap:20px; padding-top:10px; }
  .header-top{ flex-direction:column; align-items:flex-start; gap:10px; width:100%; }
  .brand{ width:100%; justify-content:space-between; }
  .brand h1{ font-size:20px; }
  .brand .subtitle{ font-size:14px; margin-top:2px; }
  .hero-content{ width:100%; }
  .profile-modal-content{ width:min(90vh,90vw); height:min(90vh,90vw); }
  .view-toggle-buttons{ gap:5px; display:flex; }
  .view-toggle-buttons .view-btn{ padding:6px; font-size:12px; width:48px; }
  .view-toggle-buttons .view-btn svg{ width:20px; height:20px; }
  .contacts-grid{ grid-template-columns:1fr; }
}

/* Force "mobile" layout when user toggles .mobile-view on body */
body.mobile-view .wrap{ max-width:420px; padding:clamp(1rem,5vw,2rem); }
body.mobile-view .grid{ grid-template-columns:1fr; } /* stack project cards */
body.mobile-view .contacts-grid{ grid-template-columns:1fr; }
body.mobile-view .floating-nav{ padding:10px 10px; }
body.mobile-view .floating-nav a{ font-size:14px; padding:6px 10px; }
body.mobile-view .view-toggle-buttons{ gap:5px; }
body.mobile-view .view-toggle-buttons .view-btn{ padding:6px; font-size:12px; width:48px; }
body.mobile-view .view-toggle-buttons .view-btn svg{ width:20px; height:20px; }

/* ========================================================================= */
/* SCROLLBAR STYLES */
/* ========================================================================= */
body{ scrollbar-width:none; }
body::-webkit-scrollbar{ width:8px; background:transparent; }
body::-webkit-scrollbar-thumb{ background-color:transparent; border-radius:10px; }
body.show-scrollbar::-webkit-scrollbar-thumb{ background-color:var(--accent); }
body.show-scrollbar{ scrollbar-width:thin; }

/* ========================================================================= */
/* SCROLL TO TOP BUTTON */
/* ========================================================================= */
#scrollTopBtn{
  position:fixed;
  bottom:80px;
  right:20px;
  padding:12px 16px;
  border:none;
  border-radius:50%;
  background:var(--accent);
  color:#04202a;
  font-size:20px;
  font-weight:bold;
  cursor:pointer;
  box-shadow:0 0 12px rgba(6,182,212,0.6);
  opacity:0;
  transform:translateY(20px);
  transition:opacity 0.3s ease, transform 0.3s ease;
  z-index:99;
}
#scrollTopBtn.show{ opacity:1; transform:translateY(0); }

/* Re-usable */
.no-margin-top{ margin-top:0 !important; }
.small{ font-size:14px; margin-bottom:12px; }
.skills-list li{ margin-bottom:8px; }

/* small layout tweak used in your local dev (optional) */
body{ padding:25px; }
.title{ color:#5C6AC4; }
